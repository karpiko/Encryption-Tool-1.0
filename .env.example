# ============================================================================
# Email SMTP Configuration (Server Admin Only)
# ============================================================================
# This configuration allows the FILE ENCRYPTION TOOL to send emails on behalf
# of users. Users do NOT need to configure anything - they just enter sender
# and receiver email addresses when using the web interface.
#
# ADMIN: Copy this file to .env and fill in your SMTP credentials
# DO NOT commit .env to git - it contains sensitive information
# ============================================================================

# SMTP Server - which email service to use
# Default: smtp.gmail.com (Gmail)
SMTP_SERVER=smtp.gmail.com

# SMTP Port - connection port
# Default: 587 (TLS - recommended), or 465 (SSL)
SMTP_PORT=587

# SMTP Username - the SERVER EMAIL ACCOUNT
# This is the email account the system will use to send all emails
# Example: noreply@example.com or your-email@gmail.com
# NOTE: Users will see their own email as the sender, but this account
#       is used for the actual sending
SMTP_USERNAME=your-server-email@gmail.com

# SMTP Password - password for the server email account
# For Gmail: REQUIRED - Use an "App Password" (not your regular password)
# See: https://support.google.com/accounts/answer/185833
# You MUST enable 2-Factor Authentication first, then generate an app password
SMTP_PASSWORD=your-16-character-app-password

# ============================================================================
# CONFIGURATION EXAMPLES FOR DIFFERENT EMAIL PROVIDERS
# ============================================================================

# GMAIL EXAMPLE:
# SMTP_SERVER=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USERNAME=noreply@gmail.com
# SMTP_PASSWORD=xxxx xxxx xxxx xxxx  (16-char app password)

# OUTLOOK EXAMPLE:
# SMTP_SERVER=smtp-mail.outlook.com
# SMTP_PORT=587
# SMTP_USERNAME=noreply@outlook.com
# SMTP_PASSWORD=your-password

# SENDGRID EXAMPLE:
# SMTP_SERVER=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_USERNAME=apikey
# SMTP_PASSWORD=SG.your-sendgrid-api-key

# AWS SES EXAMPLE:
# SMTP_SERVER=email-smtp.us-east-1.amazonaws.com
# SMTP_PORT=587
# SMTP_USERNAME=your-ses-username
# SMTP_PASSWORD=your-ses-password
